---
/**
 * @fileoverview Renders a grid of core skills utilizing the SectionLayout.
 */

import { SkillCard } from '@/components/ui/index.astro';
import { siteConfig } from '@/data/config';
import SectionLayout from '@/layouts/SectionLayout.astro';

const { skills } = siteConfig.sections;
---

<SectionLayout id={skills.id} icon="code_blocks" title="Core Expertise">
  <div class="grid grid-cols-fluid-sm gap-md" data-columns="2">
    {skills.items.map((skill) => <SkillCard payload={skill} />)}
  </div>
</SectionLayout>
